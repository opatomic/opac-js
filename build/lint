#!/bin/sh

if [ ! -f "out/node/opac.node.js" ]; then
	echo "must run build first"
	exit 1
fi

cp eslint.config.js tools/.eslintrc.js

cp out/node/opac.node.js tools
cd tools || exit 1
./node_modules/.bin/eslint opac.node.js
cd - > /dev/null
